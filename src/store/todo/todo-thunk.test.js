import { addTodoItem } from './todo-thunk.js';
import store from '../index';
// import * as utils from '../../utils'

// jest.mock('../../utils')


describe('todo thunk', () => {
    describe('addTodoItem', () => {
        it('sample test', async () => {
            expect(1).toBe(1);
        })
    })

})